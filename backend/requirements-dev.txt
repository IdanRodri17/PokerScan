# Test / development dependencies.
# CI installs these alongside requirements.txt (see .github/workflows/ci.yml).
# Local setup:  pip install -r requirements.txt -r requirements-dev.txt
pytest>=8.0
pytest-asyncio>=0.23
# httpx<0.28: Starlette 0.27's TestClient relies on httpx's `app=` shortcut,
# which httpx 0.28 removed. Pin until FastAPI/Starlette are upgraded.
httpx>=0.27,<0.28
