[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Ensure public folder is copied to dist
[build.environment]
  NODE_VERSION = "18"
